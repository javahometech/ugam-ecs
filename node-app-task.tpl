[
    {
        "cpu": ${cpu},
        "image": "${image}",
        "memory": "${memory}",
        "name": "${name}",
        "portMappings": [
            {
                "containerPort": "${port}",
                "hostPort": "${port}"
            }
        ]
    }
]